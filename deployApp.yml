---
 - name: Running app on Test-server
   hosts: all
   become: true
   tasks:
 - name: applying deploymentFile
   command: kubectl apply -f medicure-deployment.yml
 - name: applying ServiceFile
   command: kubectl apply -f medicure-service.yml
